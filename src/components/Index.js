import React from 'react';

const Index = () => (
  <div className="container">
  <p>Auto-sizing
The example below uses a flexbox utility to vertically
center the contents and changes .col to .col-auto so that
your columns only take up as much space as needed. Put another way,
 the column sizes itself based on the contents.</p>
  </div>
)

export default Index;
